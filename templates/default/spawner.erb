#!/bin/bash

set -e

. <%= @virtualenv %>/bin/activate

<%= @virtualenv %>/bin/python <%= @virtualenv %>/bin/sentry --config=<%= @config %> run_gunicorn -b <%= @host %>:<%= @port %> -w <%= @workers %> &